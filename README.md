# ship-it-skynet

> A reference matrix comparing agentic AI coding CLIs. Continuous deployment, continuous dread.

**Last updated:** 2026-07-12 &nbsp;·&nbsp; **Matrix last verified:** 2026-07-11 (all three tier-1 columns; Background tasks / scheduling row added and verified 2026-07-12)

This is not another "best AI tool 2026" listicle. It's a Rosetta stone: row-per-feature, column-per-tool lookup for developers who already use one of these CLIs and want to know what the other side has.

## Principles

- **Primary sources only.** Every non-trivial claim links to official docs, a changelog, or a commit. Nothing from Twitter.
- **Dated.** Each column has a "last verified" date. Run `/refresh` to re-verify.
- **No vibes benchmarks.** We don't rank "which writes better React." That's a blog post, not a reference.
- **Opinions live in prose, not cells.** The intro can say "Claude Code's hooks are the only implementation I'd bet a CI pipeline on." Cells just say yes / no / partial.
- **Maintainable by one person.** If updating the matrix takes more than 30 minutes, the format is wrong.

## Scope

**Tier 1** (this matrix): Claude Code, Codex CLI, Antigravity CLI (successor to Gemini CLI, whose consumer tiers sunset 2026-06-18).

**Tier 2** (considered for deep-dives, not the main matrix): Aider, Cline, Cursor CLI, Windsurf / Codeium, Continue.dev.

**Out of scope:** IDE plugins without terminal UX (Copilot), web UIs (ChatGPT, claude.ai), model-hosting services. This is about terminal-first agents.

See [`docs/glossary.md`](docs/glossary.md) for vendor-terminology mappings — half the confusion between these tools is that "agent" means five different things.

## The matrix

**Legend:** ✅ supported · 🟡 partial · ❌ not supported · ⏳ unverified (run `/refresh`). Hover a cell for a short source description; click to open the primary source.

> ⚠️ **Column migrated: Gemini CLI → Antigravity CLI (2026-06-10).** On 2026-05-19 (I/O), Google announced the [transition of Gemini CLI to Antigravity CLI][ag-blog]. Gemini CLI and the Gemini Code Assist IDE extensions stop serving free and paid **consumer** tiers on **2026-06-18**; enterprise Standard/Enterprise licenses keep access via paid API keys (and Gemini CLI is still shipping — v0.46.0 landed the morning of the migration). The Go-based Antigravity CLI (`agy`) is the successor and, per the announcement, preserves Skills, Hooks, Subagents, and Extensions (renamed "plugins"). Its docs site launched as a JS-rendered SPA we couldn't verify cell-by-cell, so the column initially cited only the [official GitHub repo][ag-repo] — linked from the announcement itself — for its verifiable README, CHANGELOG, and Releases API. That blackout is over: since 2026-07-11 antigravity.google publishes an `llms.txt` docs index and serves every doc page's raw markdown, the column now cites official doc pages as well, and the last ⏳ cells have been resolved. The fully-verified pre-migration Gemini CLI column lives in git history, and the deep-dive pages keep their Gemini CLI sections as predecessor reference for enterprise users.

| Feature                       | Claude Code                                                                          | Codex CLI                                                                              | Antigravity CLI                                                                          |
| ----------------------------- | ------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| Plan mode                     | [✅][cc-plan]                                                                        | [✅][cx-plan]                                                                          | [✅][ag-plan]                                                                            |
| Skills                        | [✅][cc-skills]                                                                      | [✅][cx-skills]                                                                        | [✅][ag-skills]                                                                          |
| Plugins / extensions          | [✅ plugins + custom marketplaces][cc-plugins]                                       | [✅ plugins + custom marketplaces][cx-plugins]                                         | [✅ plugins via GitHub installs][ag-plugins]                                             |
| Slash commands                | [✅][cc-commands]                                                                    | [✅][cx-slash]                                                                         | [✅][ag-slash]                                                                           |
| Model selection               | [✅ `/model` + `--model` + aliases][cc-model]                                        | [✅ `/model` + `--model`][cx-model]                                                    | [✅ `/model` + `--model` + `models`][ag-model]                                           |
| Sub-agents                    | [✅][cc-subagents]                                                                   | [✅][cx-subagents]                                                                     | [✅][ag-subagents]                                                                       |
| Agent teams                   | [🟡 experimental (flag-gated)][cc-teams]                                             | [❌ hierarchical only][cx-teams]                                                       | [🟡 peer messaging; teamwork in gated preview][ag-teams]                                 |
| Hooks                         | [✅][cc-hooks]                                                                       | [🟡 broad lifecycle, still bypassable][cx-hooks]                                       | [✅][ag-hooks]                                                                           |
| MCP (Model Context Protocol)  | [✅][cc-mcp]                                                                         | [✅][cx-mcp]                                                                           | [✅][ag-mcp]                                                                             |
| Custom tools                  | [✅ via MCP][cc-mcp]                                                                 | [✅ via MCP][cx-mcp]                                                                   | [✅ via MCP][ag-mcp]                                                                     |
| Web search / grounding        | [✅ built-in `WebSearch` + `WebFetch`][cc-web]                                       | [✅ built-in web search on by default][cx-web]                                         | [✅ built-in `search_web` + `read_url_content`][ag-web]                                  |
| Permission modes              | [✅ default / acceptEdits / plan / auto / dontAsk / bypassPermissions][cc-approvals] | [✅ on-request / never / untrusted][cx-approvals]                                      | [✅ request-review / proceed-in-sandbox / always-proceed / strict + rules][ag-approvals] |
| Persistent memory             | [✅ `CLAUDE.md` + auto-memory files][cc-memory]                                      | [✅ `AGENTS.md` hierarchical][cx-memory]                                               | [✅ `GEMINI.md`/`AGENTS.md` + `.agents/rules`][ag-memory]                                |
| Checkpoints / rewind          | [✅ auto-checkpoint + `/rewind`][cc-rewind]                                          | [✅ `Esc+Esc` backtrack + `codex fork`][cx-rewind]                                     | [✅ `/rewind`][ag-rewind] + [`/fork`][ag-fork] (conversation-level)                      |
| Worktrees / isolated sandbox  | [✅ worktrees + sandbox][cc-sandbox]                                                 | [✅ sandbox][cx-sandbox]                                                               | [✅ sandbox][ag-sandbox]                                                                 |
| Headless / non-interactive    | [✅ `claude -p` + `--bare`][cc-headless]                                             | [✅ `codex exec`][cx-headless]                                                         | [✅ `-p` / `--print`][ag-headless]                                                       |
| Background tasks / scheduling | [✅ background `/tasks`][cc-bg] + [`/loop` + cron tools][cc-cron]                    | [🟡 background agent threads][cx-subagents]; [scheduler in ChatGPT app only][cx-tasks] | [✅ `/tasks` panel][ag-tasks] + [`schedule` cron tool][ag-sched]                         |
| IDE integration               | [✅ VS Code, Cursor, JetBrains][cc-ide]                                              | [✅ VS Code + forks, JetBrains][cx-ide]                                                | [🟡 Antigravity 2.0 session export][ag-ide]                                              |
| Image / multimodal input      | [✅ `Ctrl+V` paste → `[Image #N]` chip][cc-image]                                    | [✅ `-i`/`--image` + paste][cx-image]                                                  | [✅ clipboard paste][ag-image]                                                           |

> All three tier-1 columns were re-verified against primary vendor docs on 2026-07-11. Two asymmetries worth flagging: Codex CLI's hook surface keeps expanding — now ten lifecycle events enabled by default, including `SubagentStart`/`SubagentStop` and compaction events, plus `updatedInput` tool-input rewriting on `PreToolUse` — but the docs still call it a guardrail rather than an enforcement boundary: `unified_exec` streaming shell, `WebSearch`, and non-MCP tool paths still aren't intercepted, so it isn't a CI-pipeline gate (see [hooks deep-dive](docs/hooks.md)); and Claude Code remains the only tier-1 CLI with a full **Agent Teams** layer (experimental, flag-gated behind `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`) where teammates run as independent sessions, share a task list, and message each other directly — Codex CLI subagents are strictly hierarchical (parent → children, no peer messaging), and Antigravity now documents inter-agent peer messaging plus an Ultra-plan-gated `/teamwork-preview` framework, but no shared task list, and its CLI-specific docs still describe only parent-delegated background workers (its predecessor Gemini CLI was a verified ❌) — which is why Agent Teams earns its own matrix row.
>
> The 2026-06-10 column swap: **Gemini CLI → Antigravity CLI** (`agy` — v1.1.1 as of the 2026-07-10 release). At swap time only fourteen of nineteen rows were verifiable, all from the official repo's README and CHANGELOG; the other five — plan mode, agent teams, hooks, web search, and checkpoints/rewind — waited on real documentation, because Gemini inheritance (`~/.gemini/` config paths, Gemini models underneath) isn't evidence. On 2026-07-11 the documentation landed — antigravity.google now publishes an `llms.txt` index and serves each doc page's raw markdown — and all five cells resolved: plan mode ✅ (one of three execution modes, with `Shift+Tab` cycling and a `--mode=plan` flag), hooks ✅ (a `hooks.json` lifecycle with hard-blocking `PreToolUse`), web search ✅ (built-in `search_web` and `read_url_content` tools), checkpoints/rewind ✅ (`/rewind` plus a conversation-level `/fork`), and agent teams 🟡 (documented peer messaging and an Ultra-plan-gated `/teamwork-preview`, but no shared task list). The Codex CLI column moved too, without a single symbol changing: OpenAI relocated the entire Codex docs tree from developers.openai.com to learn.chatgpt.com (every old URL 301s there), so all Codex citations now point at the new canonical host.

## Deep dives

Some features don't fit in a checkmark. See prose versions:

- [Skills](docs/skills.md) — how each CLI lets you package reusable procedures
- [Sub-agents & agent teams](docs/sub-agents.md) — delegated helpers vs. coordinating peers, and why the matrix carries two rows
- [Hooks](docs/hooks.md) — lifecycle events, and the "can I bet a CI pipeline on this?" question
- [MCP](docs/mcp.md) — who actually supports Model Context Protocol, and how

## Contributing

Vendor PRs accepted — but flagged `claim added by vendor, not yet independently verified` until a maintainer re-runs `/refresh` on that row. See [CLAUDE.md](CLAUDE.md) for the project conventions.

## Maintenance

Updating the matrix should take less than 30 minutes per refresh cycle. If it doesn't, the format is wrong, not the cadence. Run `/refresh` to re-verify one tool or all of them at once.

<!-- Reference-link definitions for the matrix. Hover text is the source description; URL is the primary source. -->

[cc-skills]: https://code.claude.com/docs/en/skills "Claude Code — skills live in .claude/skills/<name>/SKILL.md; follows Agent Skills open standard"
[cc-subagents]: https://code.claude.com/docs/en/sub-agents "Claude Code — sub-agents in .claude/agents/; built-in Explore, Plan, general-purpose"
[cc-teams]: https://code.claude.com/docs/en/agent-teams "Claude Code — experimental Agent Teams (CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1; docs describe behavior as of v2.1.178); team lead + peer teammates, shared task list, direct inter-agent messaging, TeammateIdle/TaskCreated/TaskCompleted hooks"
[cc-plan]: https://code.claude.com/docs/en/permission-modes "Claude Code — plan mode is one of six permission modes; Shift+Tab toggle, /plan prefix, --permission-mode plan flag; read-only while planning"
[cc-commands]: https://code.claude.com/docs/en/commands "Claude Code — built-in slash commands plus user-defined commands under .claude/commands/ (now merged into skills)"
[cc-hooks]: https://code.claude.com/docs/en/hooks "Claude Code — full lifecycle hook surface configured in settings.json; command/http/mcp_tool/prompt/agent handlers; blocking PreToolUse, PermissionRequest, and 20+ other events"
[cc-mcp]: https://code.claude.com/docs/en/mcp "Claude Code — MCP servers in .mcp.json (project scope) or ~/.claude.json (user and local scopes); mcp__<server>__<tool> tool naming flows into permission rules and hook matchers; sub-agents can scope their own MCP stack"
[cc-memory]: https://code.claude.com/docs/en/memory "Claude Code — CLAUDE.md hierarchical loading (project/user/managed/local) + auto-memory at ~/.claude/projects/<project>/memory/; first 200 lines or 25KB of MEMORY.md loaded every session"
[cc-sandbox]: https://code.claude.com/docs/en/sandboxing "Claude Code — OS-level sandbox via Seatbelt (macOS) or bubblewrap (Linux/WSL2); filesystem + network isolation with proxy; separate --worktree flag (or isolation: worktree in subagent frontmatter) for parallel git worktrees under .claude/worktrees/ (see /docs/en/worktrees)"
[cx-plan]: https://learn.chatgpt.com/docs/developer-commands "Codex CLI — /plan slash command; plan_mode_reasoning_effort config"
[cx-skills]: https://learn.chatgpt.com/docs/build-skills "Codex CLI — .agents/skills/<name>/SKILL.md; Agent Skills open standard; progressive disclosure"
[cx-slash]: https://learn.chatgpt.com/docs/developer-commands "Codex CLI — built-in slash commands documented in the linked table; user-defined via skills"
[cx-subagents]: https://learn.chatgpt.com/docs/agent-configuration/subagents "Codex CLI — subagent workflows enabled by default; custom agents as standalone TOML files under ~/.codex/agents/ (user) or .codex/agents/ (project); /agent switches between active background agent threads"
[cx-teams]: https://learn.chatgpt.com/docs/agent-configuration/subagents "Codex CLI — subagents are hierarchical only: parent orchestrates spawning, routing, waiting, and closing; no peer-to-peer messaging or shared task list; CSV batch mode is experimental"
[cx-hooks]: https://learn.chatgpt.com/docs/hooks "Codex CLI — lifecycle hooks enabled by default (SessionStart/SubagentStart at thread scope; PreToolUse/PermissionRequest/PostToolUse/UserPromptSubmit/SubagentStop/PreCompact/PostCompact/Stop at turn scope); PreToolUse can deny or rewrite tool input via updatedInput for Bash, apply_patch (Edit/Write aliases), and MCP tools — but the docs still call hooks a guardrail rather than a complete enforcement boundary (no coverage for unified_exec streaming shell, WebSearch, or non-MCP tool paths)"
[cx-mcp]: https://learn.chatgpt.com/docs/extend/mcp "Codex CLI — MCP servers in ~/.codex/config.toml (or project-scoped .codex/config.toml in trusted projects) under [mcp_servers.*] with per-tool approval overrides; managed via the codex mcp CLI"
[cx-approvals]: https://learn.chatgpt.com/docs/agent-approvals-security "Codex CLI — approval modes on-request / never / untrusted plus a granular policy object; sandbox modes read-only / workspace-write / danger-full-access"
[cc-approvals]: https://code.claude.com/docs/en/settings "Claude Code — default / acceptEdits / plan / auto / dontAsk / bypassPermissions (defaultMode setting)"
[cx-memory]: https://learn.chatgpt.com/docs/agent-configuration/agents-md "Codex CLI — AGENTS.md loaded hierarchically from ~/.codex and git root"
[cx-sandbox]: https://learn.chatgpt.com/docs/agent-approvals-security "Codex CLI — per-platform sandbox: Seatbelt (macOS), bwrap+seccomp (Linux and WSL2; WSL1 dropped in 0.115), native Windows sandbox; codex sandbox landlock survives only as a helper alias"
[ag-blog]: https://developers.googleblog.com/an-important-update-transitioning-gemini-cli-to-antigravity-cli/ "Google Developers Blog (2026-05-19, I/O) — Gemini CLI transitions to the Go-based Antigravity CLI; consumer tiers end 2026-06-18, enterprise Standard/Enterprise keeps paid-API-key access; Antigravity CLI preserves Skills, Hooks, Subagents, and Extensions (renamed plugins)"
[ag-repo]: https://github.com/google-antigravity/antigravity-cli "Antigravity CLI — official repo, linked from the transition blog post; README, CHANGELOG, and the GitHub Releases API; since 2026-07-11 antigravity.google also serves /llms.txt (docs index with a dedicated CLI section) and raw doc markdown at /assets/docs/<path>/<filename>.md (filenames come from the SPA's doc manifest and differ from URL slugs), while the /docs pages themselves remain client-rendered shells"
[ag-plan]: https://antigravity.google/docs/cli/modes "Antigravity CLI — plan is one of three execution modes (default / accept-edits / plan); Shift+Tab cycling, /plan prompt prefix, --mode=plan flag, agentMode in settings.json; read-only investigation before writing code; legacy /planning and /fast commands removed in 1.1.0"
[ag-skills]: https://antigravity.google/docs/cli/plugins "Antigravity CLI — workspace skills as .md files with name/description frontmatter in .agents/skills/, auto-converted to slash commands; global ~/.gemini/antigravity-cli/skills/; the platform skills doc separately documents the agentskills.io SKILL.md folder standard (.agents/skills/<folder>/SKILL.md, global ~/.gemini/config/skills/<folder>/)"
[ag-slash]: https://antigravity.google/docs/cli/reference "Antigravity CLI — authoritative slash-command table in the CLI reference; fuzzy matching and alias resolution per CHANGELOG 1.0.6; skills auto-convert into user-defined slash commands"
[ag-subagents]: https://antigravity.google/docs/cli/commands/agents "Antigravity CLI — custom agents at .agents/agents/<name>/agent.md (workspace) or ~/.gemini/config/agents/<name>/agent.md (global), YAML frontmatter (name, description) plus a system-prompt body; /agents panel switches custom agents and monitors background subagents; nested subagents (grandchild and deeper) per CHANGELOG 1.1.1"
[ag-teams]: https://antigravity.google/docs/subagents "Antigravity — the 2.0 Subagents doc documents inter-agent messaging (agents can communicate not only with their direct parents or subagents, but with any other active agent whose ID is known) and a Multi-Agent Teamwork framework via /teamwork-preview (preview, Ultra-plan-only); no shared task list is documented, and the CLI's own subagents page describes only parent-delegated background Subagents/Tasks"
[ag-web]: https://antigravity.google/docs/hooks "Antigravity — built-in search_web (query + optional domain) and read_url_content tools documented in the hooks tool catalog; URL fetching is governed by the read_url permission action, which defaults to Ask (see /docs/cli/permissions)"
[ag-rewind]: https://antigravity.google/docs/cli/features "Antigravity CLI — /rewind (alias /undo) rolls conversation history back to a previous checkpoint; conversation-level, file-state restore not documented"
[ag-fork]: https://antigravity.google/docs/cli/conversations "Antigravity CLI — /fork (alias /branch) clones the entire conversation history up to the current turn into a new, independent session; docs note forking clones the conversation thread, not your local git checkout"
[ag-hooks]: https://antigravity.google/docs/hooks "Antigravity — hooks.json in workspace .agents/ or global ~/.gemini/config/; PreToolUse/PostToolUse with regex tool-name matchers, PreInvocation/PostInvocation step injection (injectSteps), Stop decision continue re-enters the loop; blocking PreToolUse decisions allow/deny/ask/force_ask with permissionOverrides; command-type handlers only; the doc covers the CLI (transcriptPath ~/.gemini/antigravity-cli)"
[ag-mcp]: https://antigravity.google/docs/mcp "Antigravity CLI — global ~/.gemini/config/mcp_config.json + workspace .agents/mcp_config.json; stdio via command, remote SSE / Streamable HTTP / websocket via serverUrl (docs mark legacy url/httpUrl fields unsupported); /mcp interactive manager; disabledTools and mcp(server/tool) permission rules; OAuth/ADC/header auth"
[ag-approvals]: https://antigravity.google/docs/cli/permissions "Antigravity CLI — action(target) allow/ask/deny engine (read_file, write_file, read_url, execute_url, command, unsandboxed, mcp) with Deny > Ask > Allow precedence; toolPermission presets request-review (default) / proceed-in-sandbox / always-proceed / strict per the CLI reference settings.json table"
[ag-memory]: https://antigravity.google/docs/cli/gcli-migration "Antigravity CLI — workspace GEMINI.md and AGENTS.md context files plus global ~/.gemini/GEMINI.md; workspace rules in .agents/rules with activation modes incl. Always On / Model Decision / Glob (see /docs/rules-workflows); exclusion rules and allowlists via rules.json (CHANGELOG 1.0.4)"
[ag-sandbox]: https://antigravity.google/docs/cli/sandbox "Antigravity CLI — Terminal Sandbox backends: nsjail (Linux), sandbox-exec (macOS), AppContainer (Windows); enableTerminalSandbox setting (default false); per-command run-in-sandbox / run-without-restrictions prompts; unsandboxed(prefix) permission action; proceed-in-sandbox toolPermission option; no worktree feature documented"
[cc-plugins]: https://code.claude.com/docs/en/plugins "Claude Code — plugins bundle skills, agents, hooks, MCP + LSP servers, commands, and settings via .claude-plugin/plugin.json; distributed through marketplaces. Official claude-plugins-official is pre-registered; /plugin marketplace add accepts arbitrary GitHub repos, git URLs, local paths, or hosted marketplace.json as custom catalogs; /plugin install <name>@<marketplace> with user/project/local/managed scopes"
[cx-plugins]: https://learn.chatgpt.com/docs/build-plugins "Codex CLI — plugins bundle skills, apps (connectors), MCP servers, browser extensions, hooks, and scheduled-task templates via .codex-plugin/plugin.json. codex plugin marketplace add accepts GitHub shorthand (owner/repo[@ref]), HTTP/HTTPS or SSH Git URLs, and local paths as first-class catalogs, alongside project-scoped .agents/plugins/marketplace.json, user-scoped ~/.agents/plugins/marketplace.json, and the curated plugin directory in the ChatGPT app"
[ag-plugins]: https://antigravity.google/docs/cli/plugins "Antigravity CLI — plugins staged under ~/.gemini/antigravity-cli/plugins/<name>/ with a required plugin.json manifest ($schema at antigravity.google/schemas/v1/plugin.json); bundles skills, agents, rules, mcp_config.json, hooks.json; agy plugin list/install/enable/disable/uninstall; GitHub-subpath installs with branch resolution (1.0.7) and auto-resolved Git submodules on external installs (1.0.9); no marketplace catalog documented"
[cc-model]: https://code.claude.com/docs/en/model-config "Claude Code — /model switches mid-session; --model flag, ANTHROPIC_MODEL env, or settings.model; aliases sonnet/opus/haiku/fable/opusplan/best plus [1m] variants"
[cx-model]: https://learn.chatgpt.com/docs/developer-commands "Codex CLI — /model command picks active model and reasoning effort mid-session; --model/-m flag at launch"
[ag-model]: https://github.com/google-antigravity/antigravity-cli/blob/main/CHANGELOG.md "Antigravity CLI — --model launch flag and a models listing subcommand (1.0.5); /model command in the TUI; Gemini models underneath (tool-call limits raised for Gemini models in 1.0.7)"
[cc-web]: https://code.claude.com/docs/en/agent-sdk/typescript "Claude Code — built-in WebSearch tool (query + allowed_domains/blocked_domains) alongside WebFetch, documented in the Agent SDK tool reference"
[cx-web]: https://learn.chatgpt.com/docs/web-search "Codex CLI — first-party web search enabled by default, serves from an OpenAI-maintained cache; --search flag switches to live results"
[cc-rewind]: https://code.claude.com/docs/en/checkpointing "Claude Code — every prompt auto-creates a checkpoint; /rewind or Esc+Esc restores code, conversation, or both"
[cx-rewind]: https://learn.chatgpt.com/docs/developer-commands "Codex CLI — Esc+Esc walks back through the transcript to edit/fork a past user message; /fork clones the current conversation into a new thread; codex fork branches saved sessions"
[cc-headless]: https://code.claude.com/docs/en/headless "Claude Code — claude -p runs non-interactively; --output-format json|stream-json and --bare for clean CI starts"
[cc-bg]: https://code.claude.com/docs/en/agents "Claude Code — /tasks lists everything running in the background of the current session (shells, subagents) and lets you check on, attach to, or stop it; background Bash commands run without blocking the conversation; claude agents opens agent view for background sessions"
[cc-cron]: https://code.claude.com/docs/en/scheduled-tasks "Claude Code — /loop reruns a prompt on an interval; CronCreate/CronList/CronDelete tools schedule one-shot or recurring prompts with 5-field cron expressions, session-scoped (v2.1.72+); the page's comparison table covers /schedule cloud routines and Desktop scheduled tasks as the persistent options"
[cx-tasks]: https://learn.chatgpt.com/docs/automations "Codex CLI — no CLI-native scheduler: 'Scheduled' recurring tasks run in ChatGPT Work on the web or Work/Codex in the ChatGPT desktop app (which can use local projects and worktrees); plugins bundle scheduled-task templates 'where scheduled tasks are available'; codex exec docs point at external CI for scheduled jobs"
[ag-tasks]: https://antigravity.google/docs/cli/features "Antigravity CLI — /tasks: monitor, view logs for, or terminate active background tasks; asynchronous background Subagents and Tasks with an /agents management panel"
[ag-sched]: https://antigravity.google/docs/hooks "Antigravity — schedule tool sets timers or recurring cron jobs (DurationSeconds / CronExpression / MaxIterations / Prompt); manage_task lists, kills, checks status of, or sends input to background tasks; documented in the hooks tool catalog, which covers the CLI"
[cx-headless]: https://learn.chatgpt.com/docs/non-interactive-mode "Codex CLI — codex exec runs non-interactively: progress streams to stderr, only the final agent message prints to stdout; --json switches stdout to newline-delimited JSON events; codex exec resume continues a previous run"
[ag-headless]: https://github.com/google-antigravity/antigravity-cli/blob/main/CHANGELOG.md "Antigravity CLI — headless print mode via -p / --print with --sandbox flag propagation (1.0.6); headless metadata cached under ~/.gemini/antigravity-cli/cache (1.0.5); output-format options not yet documented"
[cc-ide]: https://code.claude.com/docs/en/vs-code "Claude Code — VS Code + Cursor extension with inline diffs, @-mentions, plan review; separate JetBrains plugin for IntelliJ, PyCharm, WebStorm"
[cx-ide]: https://learn.chatgpt.com/docs/codex/ide "Codex CLI — Codex extension for VS Code, Cursor, and Windsurf; Xcode and JetBrains IDEs ship their own integrations"
[ag-ide]: https://github.com/google-antigravity/antigravity-cli "Antigravity CLI — shared agent engine and bidirectional settings sync with the Antigravity 2.0 desktop app, plus session export from terminal to the GUI (repo README); no third-party IDE (VS Code / JetBrains) integration documented"
[cc-image]: https://code.claude.com/docs/en/interactive-mode "Claude Code — Ctrl+V / Cmd+V / Alt+V pastes clipboard images, inserting an [Image #N] chip you can reference positionally in the prompt"
[cx-image]: https://learn.chatgpt.com/docs/developer-commands "Codex CLI — attach screenshots with -i/--image flag or paste directly into the TUI composer; comma-separated multi-file support"
[ag-image]: https://github.com/google-antigravity/antigravity-cli/blob/main/CHANGELOG.md "Antigravity CLI — clipboard pipeline handles raw image data with native Wayland (wl-paste) and X11 (xclip) support, preferring copied files from file managers (1.0.7)"
